<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.colombelli.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Dino App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TesteFirebase">
            <!--<intent-filter> &lt;!&ndash; esse intent filter faz iniciar quando lanca o app &ndash;&gt;-->
                <!--<action android:name="android.intent.action.MAIN" />-->
                <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        </activity>
        <activity android:name=".TelaInicial">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CalendarioActivities" />
        <activity android:name=".AgendaActivities" />
        <activity
            android:name=".EventoActivities"
            android:label="@string/title_activity_evento_activities"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>